{
  "name": "rawbe-photography-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static photography portfolio with Nostr/Blossom photo uploads and two switchable themes.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "sync": "node scripts/sync-nostr.js",
    "sync:check": "node scripts/sync-nostr.js --dry-run",
    "serve": "python3 -m http.server 8080"
  },
  "dependencies": {
    "nostr-tools": "^2.10.0"
  }
}
